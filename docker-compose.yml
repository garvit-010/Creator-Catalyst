version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      # Streamlit specific env vars can go here if needed
      - PYTHONUNBUFFERED=1